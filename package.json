{
  "name": "micro-fronends-app",
  "version": "0.0.1",
  "description": "Entry point Create React app Running with rsbuild",
  "workspaces": [
    "./host",
    "./remote"
  ],
  "scripts": {
    "start": "pnpm --filter cra_* start",
    "build": "pnpm --filter cra_* build",
    "preview": "pnpm run build && pnpm --filter cra_* preview"
  }
}
